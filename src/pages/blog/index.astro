---
// blog/index.astro - Blog listing page
import PageLayout from '../../layouts/PageLayout.astro';
import { getCollection } from 'astro:content';

const allPosts = await getCollection('blog', ({ data }) => !data.draft);
const posts = allPosts.sort((a, b) => new Date(b.data.date).getTime() - new Date(a.data.date).getTime());
---

<PageLayout
  title="Blog"
  description="Practical legal tips, regulatory updates, and guides for expats living in Vietnam."
>

  <section class="section-spacing bg-[#FAFAF8]">
    <div class="container-page">
      <div class="max-w-3xl mx-auto">

        <!-- Header -->
        <div class="mb-10">
          <h1 class="font-heading text-3xl md:text-4xl font-bold text-[#1B3A5C] mb-3">Blog</h1>
          <p class="text-[#6B7280] text-lg">
            Practical tips, regulatory updates, and real-world guidance for expats in Vietnam.
          </p>
        </div>

        <!-- Post list -->
        {posts.length > 0 ? (
          <div class="flex flex-col gap-6">
            {posts.map(post => (
              <a
                href={`/blog/${post.id}`}
                class="group block p-6 rounded-xl border border-[#E5E7EB] bg-white hover:border-[#1B3A5C]/30 hover:shadow-md transition-all duration-200"
              >
                <!-- Tags -->
                {post.data.tags.length > 0 && (
                  <div class="flex flex-wrap gap-2 mb-3">
                    {post.data.tags.map((tag: string) => (
                      <span class="text-xs font-medium text-[#1B3A5C] bg-[#1B3A5C]/8 px-2 py-0.5 rounded-full">
                        {tag}
                      </span>
                    ))}
                  </div>
                )}

                <h2 class="font-heading text-lg font-bold text-[#1B3A5C] group-hover:text-[#E8913A] transition-colors mb-2">
                  {post.data.title}
                </h2>

                <p class="text-[#6B7280] text-sm leading-relaxed mb-3">
                  {post.data.description}
                </p>

                <div class="flex items-center gap-3 text-xs text-[#6B7280]">
                  <time datetime={post.data.date}>
                    {new Date(post.data.date).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}
                  </time>
                  <span>·</span>
                  <span>{post.data.author}</span>
                </div>
              </a>
            ))}
          </div>
        ) : (
          <div class="text-center py-16">
            <p class="text-[#6B7280]">No posts yet. Check back soon!</p>
          </div>
        )}
      </div>
    </div>
  </section>

</PageLayout>
